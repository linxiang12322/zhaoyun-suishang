# 巢雲随行 · 端到端验收测试报告

> 测试角色：项目经理视角（10 年经验验收标准）
> 测试日期：2026-08-05
> 被测版本：原型 V1.0（`main` 分支，commit `724e7e3` 基础 + 本次修复）
> 测试方式：真实 Chrome headless + CDP 自动化驱动（零依赖，脚本见 `.e2e/e2e-test.js`）

---

## 一、测试范围

| 维度 | 覆盖 |
| --- | --- |
| 页面 | 全部 10 屏（欢迎 / 首页 / 语音 / 解析 / 确认 / 排期 / 同步 / 待办 / 日历 / 我的） |
| 按钮 | 全量按钮：开始使用、文字/语音切换、粘贴、清空（二次确认）、智能整理、排期范围 4 选、重录、说完了、卡片删除/合并/拆分、优先级/同步目标下拉、仅保存待办、确认并排期、延后到明天、稍后同步、同步到日历、失败重试、复制全部内容、完成回首页、待办搜索/筛选/勾选/顺延/删除、日历月/周切换/翻月/日期点选、权限行、无痕开关、删除所有数据、底部导航 4 tab |
| 信息流转 | 输入 → 规则拆解 → 确认 → 排期 → 同步 → 待办/日历 全链路 |
| 数据流转 | localStorage（`zhaoyun_v1`：draft / range / todos / incognito / calendar）写入、恢复、清除 |

**结果：61 / 61 项断言全部通过 ✅**

---

## 二、测试发现的问题与修复

### P1-1 品牌名不一致（用户明确要求）
- **现象**：全项目使用「朝雲随行」，与目标品牌「巢雲随行」不符。
- **修复**：全局替换「朝雲」→「巢雲」，品牌信息统一为「巢雲随行」（含「秦朝雲」→「秦巢雲」，覆盖 README / index.html / app.js / styles.css）。

### P1-2 「同步到日历」为假演示，日历数据不联动（数据流转断链）
- **现象**：排期页点「同步到日历/提醒」只跳转结果页，日历页数据是硬编码的演示事件，同步结果不会进入日历，刷新后丢失——信息流转「最后一公里」断裂。
- **修复**：
  - 新增 `localStorage.calendar` 存储；同步按钮将已确认事项按日期真实写入；
  - 日历页改为「演示事件兜底 + 本地存储合并」渲染，同步项在月视图圆点、日视图日程、已同步状态中真实展示；
  - 任务日期解析（`X月X日` / 今天 / 明天）映射到 2026 年 8 月日历。

### P1-3 待办分类计数是硬编码假数据（数据分类失真）
- **现象**：待办页「全部/今天/未完成/已完成」徽标在 HTML 中硬编码为 `6/3/4/2`，与真实列表、勾选、删除、顺延操作完全脱节。
- **修复**：新增 `updateTodoCounts()`，四个分类徽标随真实数据动态计算，增删改后即时刷新。

### P2-4 确认页字段修改不生效（「采用」流转缺失）
- **现象**：确认页修改优先级 / 同步目标只弹「字段已更新（原型演示）」toast，数据未写回任务对象，后续排期、同步沿用旧值。
- **修复**：下拉变更真实写回任务 `pri` / `sync` 字段，并同步影响排期与同步结果展示。

### P3-5 其余排查（无阻塞，维持原型定位）
- 语音输入依赖浏览器 Web Speech API，Firefox 不支持时已有降级提示（原型定位，正式版需接大模型）；
- 日历固定展示 2026 年 8 月，翻月给 toast 提示（原型约束）；
- 合并/拆分为演示交互（PRD 中属 AI 精准拆解范畴，正式版需接大模型）。

---

## 三、数据分类与同步（本次增强）

1. **待办分类**：全部 / 今天 / 未完成 / 已完成 四类计数实时准确，随勾选、顺延、删除联动。
2. **同步闭环**：确认 → 排期 → 同步到日历/提醒 → 写入 `localStorage.calendar` → 日历页联动展示（月圆点 + 日详情 + 已同步标记），刷新后保留。
3. **数据一致性**：删除所有数据可整体清空（含 calendar）；无痕模式下草稿不落盘、解析后清理原文的既有行为保持。

---

## 四、回归结论

| 检查项 | 结果 |
| --- | --- |
| 静态校验（HTML 标签 / id 引用） | 通过（42 个 id 引用无缺失） |
| 规则引擎单元测试（6 组用例） | 通过（6/6） |
| 浏览器 E2E（61 项断言） | 通过（61/61） |
| 品牌改名回归 | 无「朝雲」残留 |

**验收结论：达到原型阶段交付标准，可进入下一轮（接大模型精准拆解 / 真机语音 / 正式日历 API）。**

---

## 五、如何复测

```bash
# 1. 启动静态服务
node .e2e/serve.js            # 监听 127.0.0.1:8765

# 2. 启动 headless Chrome（CDP 9222）
chrome --headless=new --remote-debugging-port=9222 --user-data-dir=./.cdp-profile about:blank

# 3. 运行 E2E
node .e2e/e2e-test.js         # 期望 61/61 通过，截图输出在 .e2e/shot_*.png
```

---

# 第二轮：LLM 精准拆解（PRD 11 章正式版路径）

> 验收日期：2026-08-05

## 交付内容

| 交付物 | 说明 |
| --- | --- |
| `llm.js` | LLM 适配层（浏览器 + Node 双端）：OpenAI 兼容对话、JSON 输出容错解析、DeepSeek/豆包/自建端点三 provider |
| `.e2e/serve.js` | 新增 `POST /api/parse` 代理：LLM key 服务端持有（`LLM_PROVIDER/LLM_BASE/LLM_MODEL/LLM_API_KEY`），未配置返回 503 |
| `test-eval-cases.js` | 精准拆解测试集 17 条：复杂口语/方言/否定改期/条件/依赖/多事项/边界 |
| `.e2e/test-eval.js` | 对比评测脚本：规则引擎 vs LLM（代理 / 直连双模式） |
| `.e2e/mock-upstream.js` | 启发式演示 LLM 上游（无 key 时让评测/前端可跑通 LLM 链路） |
| 前端集成 | 智能整理优先走 LLM，失败/超时/未配置自动回退规则引擎；确认页标注「AI 精准拆解」 |

## 评测结论

| 引擎 | 通过（17 条测试集） |
| --- | --- |
| 规则引擎 `parseByRules` | 6/17（35%） |
| LLM（演示引擎，无 key 环境） | 9/17（53%） |

- **规则引擎短板确认**：方言日期（后儿个/傍黑儿/明儿个）、否定改期残留（"不去健身房"仍生成事项）、依赖关系（dependsOn）完全不识别、口语分段错误（"下班路上"被拆成独立事项）——正是 PRD 11 章预期
- **LLM 演示引擎优势确认**：方言日期映射、否定只留改期后事项、条件语句不拆分、依赖识别均通过
- 真实 LLM 评测：配 `LLM_API_KEY` 后运行 `node .e2e/test-eval.js --llm` 或 `--llm --direct` 即可复测

## 前端链路验证（真实 Chrome）

- LLM 模式下方言输入「后儿个傍黑儿去趟银行，明儿个早起把娃送学校」→ 拆解 3 事项 → 确认页标注「AI 精准拆解」✅
- 同步写入 `localStorage.calendar`（相对日期 今天/明天/周X/周末 正确映射日号）→ 日历联动展示 ✅
- 无 key（503）时自动回退规则引擎，全流程不受影响 ✅
- 回归：E2E 61/61 通过 ✅

## 遗留事项（下一轮）

- 真实 LLM 评测需用户提供 key（DeepSeek 平台申请）后跑通
- prompt 温度/输出 schema 可进一步用真实 LLM 结果迭代（如冲突检查字段）
- 生产化：代理需加鉴权/限流，key 迁移到后端服务
